import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cafeteriaApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
